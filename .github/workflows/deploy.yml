name: deploy

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read

concurrency:
  group: deploy-docs
  cancel-in-progress: true

env:
  # Read @shuttering/error-pages (and its @shuttering/tokens dep) from GitHub
  # Packages (see .npmrc). A PAT with read:packages on the shuttering org,
  # stored as an org Actions secret. @shuttering/starlight still comes from
  # npmjs (pinned in pnpm.overrides), so npmjs stays the default registry.
  NODE_AUTH_TOKEN: ${{ secrets.SHUTTERING_NPM_TOKEN }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Dormant until infra is applied and the deploy role ARN is set as a variable.
    if: ${{ vars.AWS_DEPLOY_ROLE_ARN != '' }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-1

      - name: Sync and invalidate
        env:
          BUCKET: rackctl-docs-site
          DISTRIBUTION_ID: ${{ vars.DOCS_DISTRIBUTION_ID }}
        run: |
          aws s3 sync dist/ "s3://$BUCKET/" --delete
          aws cloudfront create-invalidation --distribution-id "$DISTRIBUTION_ID" --paths '/*'
